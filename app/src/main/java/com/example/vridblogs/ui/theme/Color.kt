package com.example.vridblogs.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)


object DarkColors {
    // Primary colors - Modern Blue/Purple gradient inspired
    val Primary = Color(0xFFCAAA0B)
    val OnPrimary = Color(0xFFFFFFFF)
    val PrimaryContainer = Color(0xFFB6A041)
    val OnPrimaryContainer = Color(0xFFF8FAFC)

    // Secondary colors - Complementary teal/cyan
    val Secondary = Color(0xFF06B6D4) // Cyan-500
    val OnSecondary = Color(0xFF0F172A)
    val SecondaryContainer = Color(0xFF0891B2) // Cyan-600
    val OnSecondaryContainer = Color(0xFFE0F7FA)

    // Tertiary colors - Warm accent (purple/pink)
    val Tertiary = Color(0xFFA855F7) // Purple-500
    val OnTertiary = Color(0xFFFFFFFF)
    val TertiaryContainer = Color(0xFF9333EA) // Purple-600
    val OnTertiaryContainer = Color(0xFFF3E8FF)

    // Error colors
    val Error = Color(0xFFEF4444) // Red-500
    val OnError = Color(0xFFFFFFFF)
    val ErrorContainer = Color(0xFFDC2626) // Red-600
    val OnErrorContainer = Color(0xFFFEF2F2)

    // Background colors - Deep dark with subtle variations
    val Background = Color(0xFF0F0F23) // Deep navy-black
    val OnBackground = Color(0xFFE2E8F0)

    // Surface colors - Layered dark surfaces
    val Surface = Color(0xFF1E1E2E) // Slightly lighter than background
    val OnSurface = Color(0xFFE2E8F0)
    val SurfaceVariant = Color(0xFF2D2D3A) // For cards, elevated elements
    val OnSurfaceVariant = Color(0xFFCBD5E1)

    // Surface containers - Different elevation levels
    val SurfaceContainer = Color(0xFF262637)
    val SurfaceContainerHigh = Color(0xFF2E2E42)
    val SurfaceContainerHighest = Color(0xFF36364A)
    val SurfaceContainerLow = Color(0xFF1A1A2E)
    val SurfaceContainerLowest = Color(0xFF141428)

    // Inverse colors
    val InverseSurface = Color(0xFFE2E8F0)
    val InverseOnSurface = Color(0xFF334155)
    val InversePrimary = Color(0xFF4338CA) // Indigo-700

    // Outline colors
    val Outline = Color(0xFF475569) // Slate-600
    val OutlineVariant = Color(0xFF64748B) // Slate-500

    // Surface tint
    val SurfaceTint = Primary

    // Scrim for overlays
    val Scrim = Color(0x80000000)
}
